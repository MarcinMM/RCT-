-# I'm thinking we should have a ruby .each for each column type thing
-# But maybe first we can just assume 3 column variable layout with modules just like d.net?
#left
	Some stuff.
#right
	Some other stuff.
#main
	Blog HAML!
	#container.generic
		Here we are in a blog, maybe.
		%form{:id => "blogForm", :method => "POST"}
			%input{:type => "text", :id => "blogTitle", :name => "blogTitle" }
			<br>
			%textarea{:id => "blogInput", :name => "blogInput" }
			<br>
			%input{:type => "submit", :value => "Submit or defy!", :id => "submit"}
			
		%h1 Well um ...
		%div 
			Title goes here
			&= @title
		- if @posts
			%p Got posts
			- @posts.each do |post|
				%h2&= post.title
				%p&= post.content
